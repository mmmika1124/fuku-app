<% if customer_signed_in? %>
  <% if @item.favorited_by?(current_customer) %>
    <%= link_to "お気に入りをはずす", customer_bookmark_path(current_customer.id), method: :DELETE, remote: true %>
  <% else %>
    <%= link_to "お気に入りに追加", customer_bookmarks_path(current_customer.id), method: :POST, remote: true %>
  <% end %>
<% end %>